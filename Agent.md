---
layout: default
title: Agent
---

# Agent

#### 基础概念与架构设计

1. 什么是 AI Agent？它和传统的 LLM 应用（如 ChatGPT）或 Workflow 有什么区别？了解过 Agent 的设计范式吗？
2. 解释一下 ReAct (Reasoning + Acting) 框架的原理。
3. 你用过哪些 AI Agent 开发框架（如 LangChain、LangGraph）？请对比它们的优缺点及框架内的核心组件。

#### 记忆机制与上下文管理

**记忆系统设计**

4. Agent 中长短期记忆如何设计？各自存什么，怎么触发读取？两者之间怎么做到协同工作？
5. 记忆机制是如何存储和管理的？在多 Agent 协作系统中，不同 Agent 之间的记忆如何实现隔离与共享？
6. 多轮对话中，如果不同轮次的记忆发生冲突，如何处理？

**上下文窗口与压缩**

7. 当对话轮数很多、上下文窗口不够时，有哪些处理策略？优先保留哪些信息？为什么？
8. 上下文窗口满了如果需要压缩，怎么去压缩？如何筛选有效信息和垃圾信息，保证正确率最大？
9. 摘要总结往往会丢失关键细节，在长文本 Agent 中一般怎么处理这一块？
10. 上下文是怎么构建的？怎么避免上下文过长或者信息污染？

**长上下文与 Attention 机制**

11. 在 Agent 多轮对话任务中，标准 Attention 机制的平方复杂度在工程落地及局限性上主要体现在哪些方面？
12. Token 过长导致的 Attention 稀释现象为什么会导致 Agent 的指令遵循能力下降？目前有哪些机制可以缓解模型在长上下文对话里的“信息遗忘”现象？

#### 规划能力与工具调用

**工具调用基础与优化**

13. 什么是 Function Calling (工具调用)？在 LangChain 中如何实现？如何提升 Agent 的 function call 能力？
14. 当候选工具超过 100 个时，如何设计路由策略？怎么解决检索过程中的召回偏差？
15. Agent 调用工具不正确怎么办？在“推理-行动”循环中，如何设计来纠正逻辑塌缩、无效工具调用或陷入思维死循环问题？

**任务规划与执行**

16. Agent 任务是怎么拆分的？拆分粒度是怎么决定的？
17. 任务执行远大于单次 Token 限制时，如何设计以支持断点继续生成？
18. 在电商或导购场景下，用户的请求往往高度模糊，Agent 怎么来精准理解需求？如何设计“主动澄清”决策逻辑？

#### 工程落地与性能优化

1. Agent 系统中的 LangGraph 是如何搭建的？其 Memory 组件的工作机制是怎样的？
2. MCP 与传统 Agent Skills 的区别是什么？如何实现在多智能体环境中动态发现并注册跨协议工具？
3. 大模型高并发调用时，如何做限流、降级和成本控制？
4. 针对包含 3 个以上工具调用且高频请求的任务，通过什么方式可以压低系统整体的端到端延迟？
5. 用户情绪异常（投诉、愤怒）时，Agent 如何在不中断主流程的情况下进行干预？

#### 评估与幻觉治理

1. 如何衡量 Agent 的 Planning 能力 vs Hallucination Rate？请列举具体的量化评估指标或自动化评估框架。
2. 为什么在复杂的 Agent 闭环场景中，仅靠 RAG 无法彻底解决幻觉问题？在项目中是如何解决 LLM“幻觉”问题的？

## Agent设计

1. 设计一个智能导购助手 Agent？描述其感知、规划、记忆和执行四大模块在分布式架构下的协同逻辑。
1. 给了一个场景，就是设计一个全自动化的agent进行AI漫剧创作，你会怎么设计？
1. 大规模 Agent 系统在多线程/多进程场景下的资源调度策略如何设计？
1. 如果让你设计一个AI Agent的工作流编排画布(类似LangFlow)，你会选择什么技术栈，核心难点在哪?
1. 假如一个Agent 推理链路包含3个工具+高频请求，系统整体延迟较高，你会如何优化？

## Prompt

1. 提示词模板是怎么设计和迭代的？你怎么判断一个模板是真的更好了？

# MCP

1. MCP了解吗？用过吗？底层是如何实现的？
2. A2A协议知道吗？

# SKILLS

1. Skill了解吗？用过吗？底层是如何实现的？比如渐进式披露是怎么实现的原理是什么？

# 手撕

1. 手撕算法： 给定一个正整数 n（如 23121和一组不重复的数字集合 a（如 \{2,4,9\}），求使用 a 中数字组成的、小于 n 的最大整数。要求考虑贪心回退逻辑，处理所有边界情况。
1. 手撕代码：编辑距离
1. 手写算法:实现一个简单的文本相似度计算函数(如余弦相似度)。

# SQL

1. 统计每个商家的评论数并去重